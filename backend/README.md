# Retired service

This directory is retained only as migration reference material. It is not a
deployable service: its `dev` and `start` scripts intentionally fail closed.

The supported public API is the same-origin Next.js API in `apps/web/src/app/api`.
Do not expose or deploy the legacy Fastify server.
